#include "main.h"

/**
  * mul - Check if that multiplies two integers
  * @a: The number of multiply
  * @b: second number multiply
  *
  * Return: Result of the multiplication
  */
int mul(int a, int b)
{
	return (a * b);
}
